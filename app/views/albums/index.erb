<!-- <h1> Albums </h1> -->

<%@albums.each do |album| %>
	<!-- <p><%= album.name %></p> -->
	<!-- hacky and wrong. Refactor -->
	<div class="album-cover"><a href="<%=album_url(album)%>"> <img src="data:image/png;base64, <%=encode(Photo.find_by(album_id: album.id))%>"></a> </div>
<%end%>