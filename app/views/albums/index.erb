<!-- <h1> Albums </h1> -->
<%@albums.each do |album| %>
	<!-- hacky and wrong. Refactor -->

	<div class="box">
		<div class = "innerBox"><img class="cover" src="data:image/png;base64, <%=encode(Photo.find_by(album_id: album.id)) if !Photo.find_by(album_id: album.id).nil?%>"></div>
		<div class ="titlebox"><%= album.name %></div>
	</div>
<%end%>


